{
  "name": "boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "fontfaceobserver": "^2.1.0",
    "imagesloaded": "^4.1.4"
  },
  "devDependencies": {
    "@parcel/transformer-glsl": "^2.0.0-beta.2",
    "@parcel/transformer-image": "^2.0.0-nightly.1981",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2",
    "three": "^0.128.0"
  },
  "scripts": {
    "serve": "rimraf dist && npx parcel index.html --no-source-maps --no-cache",
    "build": "rimraf dist && npx parcel build index.html --no-source-maps --no-cache --no-content-hash"
  },
  "author": "",
  "license": "ISC"
}
